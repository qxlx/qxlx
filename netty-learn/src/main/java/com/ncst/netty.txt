
a.Netty内存使用技巧
   1.能用基本类型就不用包装类
   2.应该定义成类变量的不要定义为实例变量  模板与人民币
   3.提前预估容器大小 eg:HashMap
   4.zero copy
   5.堆外内存  对比烧烤店满了(JVM(堆(heap)+非堆(non heap))) 只能到外边去(JVM堆外内存(off heap))
            优点.a.破除堆空间限制，减轻GC压力 b.避免复制
            缺点 a.收操作系统管理
   6.内存池  复用能力
